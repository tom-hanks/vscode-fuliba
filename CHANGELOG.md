# 更新日志

福利吧扩展的全部版本变更。格式大体沿用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.8.0] - 2026-09-16

### 新增

- **支持楼主**：楼主那一层的底部多了一块「支持楼主」——就是论坛里那个张国立竖大拇指的按钮，现在不用切浏览器就能点。
  - 赞数与论坛实时同步，点完立刻更新，并标出「今天还能支持几个」
  - 走的是论坛自己的评分接口，所以网页上的限制在这里**一模一样**：不能给自己发表的帖子评分、同一个帖子只能支持一次、当日次数用完就不能再点
  - 提交失败时直接把论坛返回的原话显示出来（比如「抱歉，您的请求来路不正确或表单验证串不符」），不吞错、不假装成功
  - 帖子 HTML 里那一块是空的 —— 论坛前端用 AJAX 往 `#btpsajax_{pid}` 里填。所以是扩展带着 Cookie 单独请求一次拿回来的，不是从页面里抠的

### 修复

- **视频没有声音，这次是彻底解决。** 上一版（0.6.0）说「音轨换成 MP3 就好了」，其实只对了一半 ——
  转码产物本身是对的（拿它单独播放，声音和音量键都正常），坏在**怎么把这个本地文件交给播放器**。

  - **病根**：VS Code 的 webview 资源代理（`file+.vscode-resource.vscode-cdn.net`）在帖子面板里读不出本地文件。
    三种送法全试过，全部撞墙：资源地址 `MEDIA_ELEMENT_ERROR: Format error`（`code=4`）、
    把 `%2B` 还原成 `+` 一样、绕道 `fetch` 则拿到 HTTP 401。这一层在扩展侧看不到也改不了。
  - **做法**：扩展自己起一个**只监听 `127.0.0.1` 的 HTTP 媒体服务**，播放器直接从它取文件，绕开资源代理。
    服务带随机路径 token（防止别的程序猜到地址）、文件名白名单（防止路径穿越）、
    `Range` / `206` / `416` / `HEAD` 支持（大文件流播必需）、`Cache-Control: no-store`，扩展停用时自动关闭。
  - **兜底**：本机 HTTP 不通时自动退回 `blob:`（`fetch` → `Blob` → `createObjectURL`）；
    两条都不通就把原始远程地址放回去，至少画面还在，并把每种送法报的错写进播放器下面那行说明。
  - 一次成功的送法会被记住，同一个帖子后面的视频直接照做，不再重试。

  顺带修掉两个会让「明明修好了却看不出来」的坑：

  - 同一个视频被贴在多个楼层时，以前只换第一个播放器的源。如果你正在看的是后面那层，就会以为修复没生效 —— 现在全部一起换。
  - 换源时显式解除静音（`muted` / `defaultMuted` / `volume`）。论坛模板给 `<video>` 挂 `muted autoplay` 很常见，
    而 `muted` 是属性级的，光换 `src` 不会让它失效 —— 表现正是「画面正常、换了音轨还是没声音」。

### 变更

- **播放器拖动收尾兜底**：拖完尺寸后，整页播放器一度会变得点不动（连播放键都按不了，只能刷新）。
  原因是拖动期间会给播放器关掉指针事件，而「收尾」只挂在拖拽手柄自己的 `pointerup` 上 ——
  指针滑出面板再松开、被系统抢走、或窗口失焦时，这个收尾根本不会执行，状态就永久留在页面上。
  现在在 `window` 层也接住了 `pointerup` / `pointercancel` / `blur`。
- 视频转码的**进度与结果说明**更明确了：能自动修就直接修，失败时说明里会带上具体是哪种送法出的什么错。

## [0.6.0] - 2026-09-15

### 新增

- **版块内排序**：最新发布 / 最新回复 / 热帖，侧边栏标题栏的「排序方式」按钮切换
- **屏蔽置顶帖**：置顶帖常年霸占列表前排，一个开关就能让它们消失
- **选择显示的版块**：版块树可以自己勾选保留哪些，默认全部显示
- **播放器可拖动缩放**：播放器右上角有一块斜纹，按住斜着拖就能放大缩小。
  **改一次全站通用** —— 所有帖子里的播放器（包括还开着的其它标签页）一起跟着变，尺寸记在设置里，下次打开还是这个大小

### 修复

- **视频没有声音**（第一版方案）：VS Code 的 webview 跑在 Electron 自带的 Chromium 上，那份构建不含 AAC 解码器，
  所以论坛直传的 mp4 全是「有画面没声音、音量键还灰着」。改成打开帖子时**自动**用 ffmpeg 把音轨换成 MP3
  再封装回 mp4（画面轨 `-c:v copy` 原样搬运，不重编码、不掉帧），结果按视频缓存。
  需要本机有 ffmpeg，详见 [README 的说明](README.md#关于视频没有声音)。
- 修 `socket hang up`，以及没插进正文的图片附件会丢失的问题

## [0.2.1] - 2026-09-15

### 变更

- 重写描述与介绍，定位换成「码农摸鱼神器」

## [0.2.0] - 2026-09-15

### 变更

- 换用葫芦娃图标

## [0.1.0] - 2026-09-15

### 新增

- 首个版本：版块树浏览、帖子详情、图片按需加载、已读标记、搜索

[0.8.0]: https://github.com/tom-hanks/vscode-fuliba/compare/v0.6.0...v0.8.0
[0.6.0]: https://github.com/tom-hanks/vscode-fuliba/compare/v0.2.1...v0.6.0
[0.2.1]: https://github.com/tom-hanks/vscode-fuliba/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/tom-hanks/vscode-fuliba/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/tom-hanks/vscode-fuliba/releases/tag/v0.1.0
